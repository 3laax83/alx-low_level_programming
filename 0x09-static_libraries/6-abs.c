#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * _abs - computes the absolute
 * value of an integer.
 * @num : number
 *
 * Description: computes the
 * absolute value an integer.
 *
 * Return: abs(int).
 */
int _abs(int num)
{
	return (abs(num));
}
