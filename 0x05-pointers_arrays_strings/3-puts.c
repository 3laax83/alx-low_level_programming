#include "main.h"

/**
 * _puts - printer function.
 *
 * Description: a function that
 * prints a string, followed by
 * a new line, to stdout.
 *
 * Return: None.
 */

void _puts(char *str)
{
	puts(str);
}
