#include "3-calc"

#define op_add(int a, int b) ((a) + (b))
#define op_sub(int a, int b) ((a) - (b))
#define op_mul(int a, int b) ((a) * (b))
#define op_div(int a, int b) ((a) / (b))
#define op_mod(int a, int b) ((a) % (b))
